{
  "pluginAlias": "SwitchBot",
  "pluginType": "platform",
  "singular": true,
  "customUi": true,
  "headerDisplay": "<p align='center'><img width='100px' src='https://user-images.githubusercontent.com/9875439/102681754-ac2eb800-4189-11eb-8e17-7c47c804484b.png'></p>\n\nThe **SwitchBot** plugin allows you to control SwitchBot Devices. \n\nTo get started link your SwtichBot account.\nTo get started link your SwtichBot account.\n\n1. Download SwitchBot App on App Store or Google Play Store.\n\n2. Register a SwitchBot account and log in into your account.\n\n3. Generate an Open Token within the App.\n\n4. Input your `Open Token` into the paramter below.",
  "footerDisplay": "Please raise any issues on our [project page](https://github.com/OpenWonderLabs/homebridge-switchbot/issues).\n\nIf you would like to have other SwitchBot Devices added to this plugin fill out [Feature Request Form](https://github.com/OpenWonderLabs/homebridge-switchbot/issues/new?assignees=&labels=&template=feature_request.md).\n\n[SwitchBot Website](https://www.switch-bot.com)",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "title": "Name",
        "default": "SwitchBot",
        "required": true
      },
      "credentials": {
        "type": "object",
        "properties": {
          "openToken": {
            "title": "Token",
            "type": "string",
            "required": true,
            "x-schema-form": {
              "type": "password"
            }
          },
          "notice": {
            "title": "Notice",
            "type": "string",
            "required": true,
            "default": "Keep your token a secret!"
          }
        }
      },
      "options": {
        "type": "object",
        "required": false,
        "properties": {
          "devices": {
            "type": "array",
            "items": {
              "title": "Devices",
              "type": "object",
              "properties": {
                "type": {
                  "title": "Device Type",
                  "type": "string",
                  "required": true,
                  "oneOf": [
                    {
                      "title": "Bot",
                      "enum": [
                        "Bot"
                      ]
                    },
                    {
                      "title": "Meter",
                      "enum": [
                        "Meter"
                      ]
                    },
                    {
                      "title": "Contact Sensor",
                      "enum": [
                        "Contact Sensor"
                      ]
                    },
                    {
                      "title": "Motion Sensor",
                      "enum": [
                        "Motion Sensor"
                      ]
                    },
                    {
                      "title": "Humidifier",
                      "enum": [
                        "Humidifier"
                      ]
                    },
                    {
                      "title": "Curtain",
                      "enum": [
                        "Curtain"
                      ]
                    },
                    {
                      "title": "Bulb",
                      "enum": [
                        "Fan"
                      ]
                    },
                    {
                      "title": "IR Fan",
                      "enum": [
                        "Fan"
                      ]
                    },
                    {
                      "title": "DIY IR Fan",
                      "enum": [
                        "DIY Fan"
                      ]
                    },
                    {
                      "title": "IR Air Conditioner",
                      "enum": [
                        "Air Conditioner"
                      ]
                    },
                    {
                      "title": "DIY IR Air Conditioner",
                      "enum": [
                        "DIY Air Conditioner"
                      ]
                    },
                    {
                      "title": "Others",
                      "enum": [
                        "Others"
                      ]
                    }
                  ]
                },
                "deviceId": {
                  "title": "Device ID",
                  "type": "string",
                  "required": true,
                  "placeholder": "81F3UT59513F",
                  "condition": {
                    "functionBody": "return (model.options && model.options.devices && model.options.devices[arrayIndices].type);"
                  }
                },
                "hide_device": {
                  "title": "Hide Device",
                  "type": "boolean",
                  "condition": {
                    "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].ble && model.options.devices[arrayIndices].type && model.options.devices[arrayIndices].deviceId);"
                  }
                },
                "ble": {
                  "title": "Enable BLE Connection",
                  "type": "boolean",
                  "required": true,
                  "condition": {
                    "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type && model.options.devices[arrayIndices].deviceId);"
                  }
                },
                "bot": {
                  "type": "object",
                  "properties": {
                    "mode": {
                      "title": "Mode",
                      "type": "string",
                      "oneOf": [
                        {
                          "title": "Press",
                          "enum": [
                            "press"
                          ]
                        },
                        {
                          "title": "Switch",
                          "enum": [
                            "switch"
                          ]
                        }
                      ],
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Bot' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "switch": {
                      "title": "Display as Switch",
                      "type": "boolean",
                      "description": "This allows you to display bot as a switch.",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Bot' && model.options.devices[arrayIndices].deviceId && model.options.devices[arrayIndices].bot && model.options.devices[arrayIndices].bot.mode);"
                      }
                    }
                  }
                },
                "meter": {
                  "type": "object",
                  "properties": {
                    "hide_temperature": {
                      "title": "Hide Meter's Temperature Sensor",
                      "type": "boolean",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Meter' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "hide_humidity": {
                      "title": "Hide Meter's Humidity Sensor",
                      "type": "boolean",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Meter' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "unit": {
                      "title": "Convert your Meter's Temperature to Celsius or Fahrenheit",
                      "type": "number",
                      "oneOf": [
                        {
                          "title": "Celsius",
                          "enum": [
                            0
                          ]
                        },
                        {
                          "title": "Fahrenheit",
                          "enum": [
                            1
                          ]
                        }
                      ],
                      "required": false,
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Meter' && model.options.devices[arrayIndices].deviceId && model.options.devices[arrayIndices].meter  && !model.options.devices[arrayIndices].meter.hide_temperature);"
                      }
                    }
                  }
                },
                "humidifier": {
                  "type": "object",
                  "properties": {
                    "hide_temperature": {
                      "title": "Hide Humidifier's Temperature Sensor",
                      "type": "boolean",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Humidifier' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "set_minStep": {
                      "title": "Set Min Step",
                      "type": "number",
                      "placeholder": "1",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Humidifier' && model.options.devices[arrayIndices].deviceId);"
                      }
                    }
                  }
                },
                "curtain": {
                  "type": "object",
                  "properties": {
                    "disable_group": {
                      "title": "Disable Grouping",
                      "type": "boolean",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Curtain' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "refreshRate": {
                      "title": "Refresh Rate",
                      "type": "number",
                      "minimum": 1,
                      "placeholder": 5,
                      "description": "Indicates the number of seconds between before refreshing Curtain status while updating slide progress.",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Curtain' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "set_minStep": {
                      "title": "Set Min Step",
                      "type": "number",
                      "placeholder": "1",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Curtain' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "set_min": {
                      "title": "Set Min",
                      "type": "number",
                      "placeholder": "0",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Curtain' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "set_max": {
                      "title": "Set Max",
                      "type": "number",
                      "placeholder": "100",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Curtain' && model.options.devices[arrayIndices].deviceId);"
                      }
                    }
                  }
                },
                "bulb": {
                  "type": "object",
                  "properties": {
                    "set_minStep": {
                      "title": "Set Min Step",
                      "type": "number",
                      "placeholder": "1",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Bulb' && model.options.devices[arrayIndices].deviceId);"
                      }
                    }
                  }
                },
                "fan": {
                  "type": "object",
                  "properties": {
                    "swing_mode": {
                      "title": "Enable Swing Mode by Device ID",
                      "type": "boolean",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && (model.options.devices[arrayIndices].type === 'Fan' || model.options.devices[arrayIndices].type === 'DIY Fan') && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "rotation_speed": {
                      "title": "Enable Rotation Speed by Device ID",
                      "type": "boolean",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && (model.options.devices[arrayIndices].type === 'Fan' || model.options.devices[arrayIndices].type === 'DIY Fan') && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "set_minStep": {
                      "title": "Set Min Step",
                      "type": "number",
                      "placeholder": "1",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && (model.options.devices[arrayIndices].type === 'Fan' || model.options.devices[arrayIndices].type === 'DIY Fan') && model.options.devices[arrayIndices].deviceId && model.options.devices[arrayIndices].fan  && !model.options.devices[arrayIndices].meter.rotation_speed);"
                      }
                    },
                    "set_min": {
                      "title": "Set Min",
                      "type": "number",
                      "placeholder": "0",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && (model.options.devices[arrayIndices].type === 'Fan' || model.options.devices[arrayIndices].type === 'DIY Fan') && model.options.devices[arrayIndices].deviceId && model.options.devices[arrayIndices].fan  && !model.options.devices[arrayIndices].meter.rotation_speed);"
                      }
                    },
                    "set_max": {
                      "title": "Set Max",
                      "type": "number",
                      "placeholder": "100",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && (model.options.devices[arrayIndices].type === 'Fan' || model.options.devices[arrayIndices].type === 'DIY Fan') && model.options.devices[arrayIndices].deviceId && model.options.devices[arrayIndices].fan  && !model.options.devices[arrayIndices].meter.rotation_speed);"
                      }
                    }
                  }
                },
                "irair": {
                  "type": "object",
                  "properties": {
                    "hide_automode": {
                      "title": "Hide Auto Mode on IR Air Conditioners",
                      "type": "boolean",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && (model.options.devices[arrayIndices].type === 'Air Conditioner' || model.options.devices[arrayIndices].type === 'DIY Air Conditioner') && model.options.devices[arrayIndices].deviceId);"
                      }
                    }
                  }
                },
                "other": {
                  "type": "object",
                  "properties": {
                    "deviceType": {
                      "title": "What Type of Device do you want to display in the Home App?",
                      "type": "string",
                      "oneOf": [
                        {
                          "title": "Fan",
                          "enum": [
                            "Fan"
                          ]
                        }
                      ],
                      "required": false,
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Others' && model.options.devices[arrayIndices].deviceId);"
                      }
                    },
                    "commandOn": {
                      "title": "Command sent to turn On Device.",
                      "type": "string",
                      "placeholder": "On",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Others' && model.options.devices[arrayIndices].deviceId && model.options.devices[arrayIndices].other  && model.options.devices[arrayIndices].other.deviceType);"
                      }
                    },
                    "commandOff": {
                      "title": "Command sent to turn Off Device.",
                      "type": "string",
                      "placeholder": "Off",
                      "condition": {
                        "functionBody": "return (model.options && model.options.devices && !model.options.devices[arrayIndices].hide_device && model.options.devices[arrayIndices].type === 'Others' && model.options.devices[arrayIndices].deviceId && model.options.devices[arrayIndices].other  && !model.options.devices[arrayIndices].other.deviceType);"
                      }
                    }
                  }
                }
              }
            }
          },
          "refreshRate": {
            "title": "Refresh Rate",
            "type": "number",
            "minimum": 120,
            "placeholder": 900,
            "description": "Indicates the number of seconds between polls of SwitchBot API."
          },
          "pushRate": {
            "title": "Push Rate",
            "type": "number",
            "placeholder": 1,
            "description": "Indicates the number of seconds between pushes to SwitchBot API."
          },
          "debug": {
            "title": "Logging Setting",
            "type": "string",
            "description": "Enble 'Device' or 'Debug' logging.",
            "oneOf": [
              {
                "title": "Enable Debug Logging",
                "enum": [
                  "debug"
                ]
              },
              {
                "title": "Enable Device Logging",
                "enum": [
                  "device"
                ]
              }
            ],
            "required": false
          }
        }
      }
    }
  },
  "layout": [
    {
      "type": "fieldset",
      "title": "SwitchBot Account Info",
      "expandable": true,
      "expanded": false,
      "items": [
        "credentials.openToken"
      ]
    },
    {
      "key": "options.devices",
      "notitle": false,
      "add": "Add Another Device",
      "type": "fieldset",
      "title": "Optional Device Settings",
      "expandable": true,
      "expanded": false,
      "orderable": false,
      "buttonText": "Add Device",
      "items": [
        {
          "type": "div",
          "displayFlex": true,
          "flex-direction": "column",
          "items": [
            "options.devices[].type",
            "options.devices[].deviceId",
            "options.devices[].hide_device",
            "options.devices[].ble",
            "options.devices[].bot.mode",
            "options.devices[].bot.switch",
            "options.devices[].meter.hide_temperature",
            "options.devices[].meter.hide_humidity",
            "options.devices[].meter.unit",
            "options.devices[].humidifier.set_minStep",
            "options.devices[].humidifier.hide_temperature",
            "options.devices[].curtain.disable_group",
            "options.devices[].curtain.refreshRate",
            "options.devices[].curtain.set_minStep",
            "options.devices[].curtain.set_min",
            "options.devices[].curtain.set_max",
            "options.devices[].bulb.set_minStep",
            "options.devices[].fan.rotation_speed",
            "options.devices[].fan.set_minStep",
            "options.devices[].fan.set_min",
            "options.devices[].fan.set_max",
            "options.devices[].fan.swing_mode",
            "options.devices[].irair.hide_automode",
            "options.devices[].other.deviceType",
            "options.devices[].other.commandOn",
            "options.devices[].other.commandOff"
          ]
        }
      ]
    },
    {
      "type": "fieldset",
      "title": "Advanced Settings",
      "expandable": true,
      "expanded": false,
      "items": [
        {
          "type": "help",
          "helpvalue": "<h5>Refresh Rate</h5><em class='primary-text'>Refresh Rate indicates the number of seconds between polls of SwitchBot API.</em>"
        },
        {
          "key": "options.refreshRate",
          "notitle": true
        },
        {
          "type": "help",
          "helpvalue": "<h5>Push Rate</h5><em class='primary-text'>Push Rate indicates the number of seconds between pushes to SwitchBot API, Currently only for Curtains.</em>"
        },
        {
          "key": "options.pushRate",
          "notitle": true
        },
        "options.debug"
      ]
    }
  ]
}